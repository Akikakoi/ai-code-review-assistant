# 2026-09-15 · acra（ai-code-review-assistant）

工作目录 `D:\AIProjects\ai-code-review-assistant`。本次任务：清掉「接口就位但从未端到端验证」清单，
顺序 CI → publish 端到端 → L3 → 增量成本 → Webhook。

## 环境事实（踩过就记下来）

- 项目此前**不是 git 仓库**（无 `.git`），远端 `git@github.com:Akikakoi/ai-code-review-assistant.git`
  是空仓库。本次已 `git init` + 首次推送（132 文件），`main` 已建立。
- 本机**没有任何 GitHub API 凭据**：无 `gh` CLI、无 credential helper、无 `GITHUB_TOKEN`。
  SSH key 可用于 git 传输，但发 review 走 REST 需要 Bearer token。
- 代理不一致：git 配 `127.0.0.1:7890`（没在跑）→ git https 失败；
  环境变量 `HTTPS_PROXY=127.0.0.1:9322`（在跑）→ httpx 正常，`api.github.com` 200。
- MiMo key 在 `.env` 里是真的；`LLM_INPUT/OUTPUT_PRICE_MICROS_PER_MTOK=0`，所以成本统计目前恒为 0
  （做"增量成本"那项时必须先填单价，否则测不出成本只能测 token）。

## 已完成

### 1. CI（item 1）

- 新增 `.github/workflows/ci.yml`：4 个任务，阻断/信息性在任务名里写清 ——
  `checks`（阻断，ruff+pytest）、`gate-e2e`（阻断，退出码契约）、
  `offline-eval`（信息）、`self-review`（信息，需 `LLM_API_KEY`）。
- 新增 `scripts/e2e_gate.py`：7 个场景、真实子进程断言退出码 0/1/2/3，
  且要求"rc=1 必须由一条真实 `[高]` 结论触发"，否则判 FAIL（防空跑假通过）。
- `.gitignore` 增加 `examples/demo-repo/`、`examples/static-demo/`：
  它们是 `build_*.py` 生成的、内部含自己的 `.git`，提交会变成 gitlink。
  门禁脚本改为自己构造临时仓库，不依赖它们被提交。
- 基线：ruff 通过、pytest **494 项全绿**（原 479 + 新增 15）。
- 修 README 里重复的空标题 `### 静态分析（真实 Ruff）`。

脚本抓到两处真问题（重要）：
1. 首次运行 4 个场景全返回 2 —— 脚本自己漏了 `review` 子命令。CLI 此前**没有任何单测**。
2. `--pr` 与 `--base/--head` 硬性互斥，导致**无法既指定发布目标又指定要审的 diff**。
   已改：非发布场景维持互斥，`--publish` 时 `--pr` 只作发布目标。

### 2. publish 层（item 2 的实现部分，未完成真实 E2E）

发现比"未验证"更严重：**`GithubPublisher` 只被测试构造过**，
`cli.review` / webhook worker / `acra worker` 三条入口都不传 `publisher`，
`pipeline` 里那段发布逻辑永远不成立。归档为 `docs/adr/0012`。

- 新增 `src/acra/github_app.py`：App JWT(RS256，iat 回拨 60s、exp 9 分钟) →
  `POST /app/installations/{id}/access_tokens`，按 installation 缓存、剩余 <5 分钟刷新。
- 新增 `src/acra/publish/factory.py`：`resolve_access()` + `open_publisher()`；
  无凭据产出 `None`（= 不发布），但用 `GithubAccess.source/error` 区分
  「本来就不用发」与「本该能发却没发成」。
- 接入 `main.py` webhook worker、`acra worker`；CLI 新增 `--publish` / `--repo-full-name`。
- `run_review(clone_token=...)` → `_open_repo` 传 token（私有仓库此前连 clone 都过不去）。
- settings 新增 `github_api_base` / `github_app_installation_id` / `acra_github_token`；
  `pyproject.toml` 显式加 `pyjwt>=2.8`（此前只是传递依赖）。
- 测试 `tests/unit/test_github_app.py` 15 项，全离线（respx）。

**待用户提供**：GitHub App 的 App ID / 私钥 `.pem` 路径 / Installation ID。
用户选了「App 还没建」，需要先按步骤建 App 并安装到仓库，
最小权限：Pull requests: write、Checks: write、Contents: read、Metadata: read。

### git 提交

- `d221c7e ci: 建立 CI 工作流与门禁退出码端到端自检`
- `7b8978b feat(publish): 实现 GitHub App 认证并接入发布路径`
- `0c775b7 test(ci): 门禁自检扩展到 --publish 契约（7 个场景）`

## 未完成（下一步）

- **item 2 publish 真实 E2E**：等 App 凭据 → 建测试 PR → `--publish` → 断言评论 → 关 PR（文档 §14.4）。
- **item 3 L3**：`src/acra/context/retriever.py` 是纯骨架（`enabled=False`，三方法恒返回 `[]`），尚未动。
- **item 4 增量成本**：文档 §16 要求「重复 push 成本下降 ≥60%」，未实测；
  注意先填 LLM 单价否则只能测 token 降幅。
- **item 5 Webhook**：验签只被合成 payload 单测过；需起服务投递真实签名请求
  （且现在 worker 已接 publisher，全链路终点终于是"真的发评论"）。
- CI 工作流已推送，但尚未观察到 Actions 里的首个真实 run（本地无 API 凭据查不到）。

## 环境坑（已写入 skill `workbuddy-win-python-devloop` §8~§10）

- git 代理 `7890` 与进程代理 `9322` 不一致；能用 SSH 就用 SSH。
- **同一文件并发 Edit 会互相覆盖**：两边都报成功但内容静默丢失。
  本次 `cli.py` 的 import 就这样被冲掉，触发 6 个 F821。必须串行改同一文件。
- 批量 git 操作约 14s/用例，101 用例 ≈ 25 分钟，直接后台跑。
