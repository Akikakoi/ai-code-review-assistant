# ---- secrets / local env ----
.env
*.pem
*.key

# ---- 本地工作记录 ----
# .workbuddy/ 存的是 agent 的工作日志与项目笔记（环境事实、缺陷分析、排查过程）。
# 它对本机开发有用，但不属于这个仓库的产物：本仓库是 public 的，
# 而这类笔记会随工作不断变化，混进提交历史只会制造噪音与信息泄露面。
# 注意：本目录**不是缓存**，不要删除它。
.workbuddy/

# ---- python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# ---- 示例仓库 ----
# 由 examples/build_demo_repo.py / build_static_demo.py 生成，本身是含 .git 的仓库。
# 不忽略的话 git 会把它们记成 gitlink（伪 submodule），而内容随时可再生产；
# 因而 CI 里的门禁自检改为自己构造临时仓库，不依赖这两个目录是否被提交。
examples/demo-repo/
examples/static-demo/
examples/l3-demo/

# ---- runtime ----
.acra-work/
*.db
*.sqlite3
logs/
eval/report*.json
findings*.json
findings*.sarif

# ---- editor / os ----
.idea/
.vscode/
.DS_Store
Thumbs.db
